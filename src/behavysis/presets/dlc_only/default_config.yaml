# =============================================================================
# DLC Only — Keypoint Tracking
# =============================================================================
#
# Format video and run DLC keypoint tracking. No analysis or behaviour
# classification — just produce keypoints. Use this when you only need
# pose estimation output.
# =============================================================================

format_video:
  width_px: 960
  height_px: 540
  fps: 15.0
  start_sec: null
  stop_sec: null

run_dlc:
  model_fp: /absolute/path/to/DLC_model/config.yaml

calculate_parameters: null

preprocess: null

extract_features: null

classify_behaviour: null

analyse: null
